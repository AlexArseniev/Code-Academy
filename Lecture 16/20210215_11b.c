
#include <stdio.h>

int main(){
    int arr[3] = { 5, 10, 20 };
    int *ptr;
    ptr = arr ; //Какво ще стане ако изпусна този ред и не задам стойност на поинтера? Опитайте! - Segmetation fault 
    for (int i = 0; i < 3; i++) {
        //printf("%d ",*ptr); -> тук принтира стойността на поредния индекс от масива;
        //ptr++; тук променяме адреса на пойнтера; 
         printf("%d ", *ptr++); // -> тук принтираме стойност и променяме адреса на един ред.
    }
    return 0;
}
