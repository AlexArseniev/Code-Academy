#include <stdio.h>

int main(void){

    int c;
    int counter = 0;

    while ((c = getchar()) != EOF){
        
        if(c == '\n'){
            counter++;
        }
    }

    printf("%d", counter);

    return 0;
}
