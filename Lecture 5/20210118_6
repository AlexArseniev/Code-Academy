#include <stdio.h>
int main(){

int i = 0, j = 0;
printf("i = %d, j = %d\n", i, j);
printf("j=i++: %d\n", j=i++); //j=0. Постинкрементацията се извършва на следващия ред. Затова j oстава 0;
printf("i = %d, j = %d\n", i, j);// i=1, j=0
printf("j = ++i: %d\n", j=++i);// j=2 Тук инкрементацията е с префикс и се случва на същия ред. j присвоява стойността на i;
printf("i = %d, j = %d\n", i, j);//i=2, j=2
printf("i--: %d\n", i--);// i=2 На този ред i e все още със стойност 2, защото постинкрементацията ще се изпълни на следващия ред.
printf("i = %d, j = %d\n", i, j);// i=1, j=2

return 0;
}
