#include <stdio.h>

int main(void){

int x = 1, y = 0;
int z = y || x; // z e равно на 1, защото проверката минава през y, което ни връща 0 и след това проверява x, което ни връща 1. 

x = 1 + 2, 2 * 3, 3 + 4; // Тук се присвоява стойността само на 1+2, защото е поредица от изрази и не се изчислява стойност на последните 2.
y = (7 * 8, 8 + 9, 9 - 4); // Тук се присвоява стойността на 9 - 4, защото е последователност от изрази в скоби, разделени със запетаи, която изчислява до последния израз. 
printf("\nX= %d, Y= %d\n", x, y);
return 0;
}
