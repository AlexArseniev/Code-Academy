#include <stdio.h>

int main(void){

    int x = 10;
    int y = 10;
    int z = 30;

    int true = x == y + (z < y) != 20; 

    /*  () са с най-висок приоритет. z < y е грешно и ни връща 0. 
        Бинарният оператор за събиране е с по-висок приоритет от операторите за сравнение != и ==, затова първо ще се се събере 10+0;
        Сега имаме 2 оператора с равни приоритети != и ==. 
        x == y е вярно и ни връща 1;
        1 се различава от 20, което ни връща вярно.
    */


    printf("This statement is true: %d\n", true);

    return 0;
}
