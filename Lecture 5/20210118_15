#include <stdio.h>

int main(void){
    int nA;
    printf("Please, entere a number:\n");
    scanf("%d", &nA);
    int nB = ( nA == 1 ? 2 : 0 ); 
    printf("A value is %d\n", nA); 
    printf("B value is %d\n",nB);

    (nA > nB ? printf("A is bigger") : printf("B is bigger"));
    return 0;
}
