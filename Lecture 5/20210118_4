#include <stdio.h>

int main(void){
    int x = 10, y = 10;
    int true = x && y & x << 1;
    /*1)Първо ще се изпълни bitwise shift.
     1010 << 1 =  0001 0100;
    
    2) След това ще се изпълни побитовото и (&);
        0001 0100; 
        0000 1010;
       =0000 0000;

    3) Последно ще се изпълни логическото и (&&);
       Ако една от двете страни на логическото И е 0, резултатът е 0 => true = 0; 
    */
    printf("%d\n", true);


    true = x && y ^ x << 1;
    /*
    1) Първо ще се изпълни bitwise shift.
        1010 << 1 = 0001 0100;
    2) След това ще се изпълни XOR (^);
        0001 0100
        0000 1010
        1 1110;
    3) Последно ще се изпълни логическото и (&&);
    И двете страни на логическото и са по-големи от 0 и това ще ни върне 1;
    */
    printf("%d", true);
    return 0;
}
